# Raspberry Pi — GUI graphs and data processing (optional)
#   .venv/bin/python -m pip install -r requirements-pi-viz.txt
#
# ปักเวอร์ชันขั้นต่ำเพื่อลดอาการเสียหายของ wheel บน Python 3.12/3.13 + ARM
# (เช่น AttributeError: _pandas_datetime_CAPI หลังติดตั้งค้าง/อัปเกรดครึ่งๆ กลางๆ)
numpy>=2.1.0
pandas>=2.2.3
matplotlib>=3.9.0
