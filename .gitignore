# Python
.venv/
.venv-hr/
venv/
__pycache__/
*.py[cod]
*$py.class

# hyperresearch (local vault caches; markdown notes stay tracked)
research/*.db
research/*.db-*
research/temp/
.hyperresearch/*.db
.hyperresearch/*.db-*

# Sensor / acquisition output (machine-local, regenerable)
reading/data/*.csv
reading/data/*.npz
!reading/data/.gitkeep
acquisition/processed_data/*
!acquisition/processed_data/.gitkeep

# Cloud upload (secrets / machine-local)
program/cloud_config.json
cloud/upload_queue.json
credentials.json
token.json
**/gdrive_service_account.json
**/*service_account*.json

# Cursor IDE (local only)
.cursor/

# OS
.DS_Store
Thumbs.db
